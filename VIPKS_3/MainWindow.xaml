<Window x:Class="VIPKS_3.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:VIPKS_3"
        mc:Ignorable="d"
        Title="Управление студентами" Height="600" Width="900" Loaded="Window_Loaded">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="База данных студентов" FontSize="16" FontWeight="Bold" VerticalAlignment="Center"/>
            <Button x:Name="btn_Add" Content="Добавить" Margin="20,5,5,5" Padding="10,5" Click="btn_Add_Click"/>
            <Button x:Name="btn_Edit" Content="Редактировать" Margin="5" Padding="10,5" Click="btn_Edit_Click"/>
            <Button x:Name="btn_Delete" Content="Удалить" Margin="5" Padding="10,5" Click="btn_Delete_Click"/>
            <Button x:Name="btn_Refresh" Content="Обновить" Margin="5" Padding="10,5" Click="btn_Refresh_Click"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBox x:Name="tb_Search" Width="200" Margin="0,0,10,0" Padding="5" 
                     TextChanged="tb_Search_TextChanged" 
                     ToolTip="Поиск по ФИО или группе"/>
            <ComboBox x:Name="cb_Filter" Width="120" Margin="0,0,10,0"
                      SelectionChanged="cb_Filter_SelectionChanged">
                <ComboBoxItem Content="Все студенты"/>
                <ComboBoxItem Content="Очная форма"/>
                <ComboBoxItem Content="Заочная форма"/>
            </ComboBox>
            <Button x:Name="btn_ClearSearch" Content="Очистить" Click="btn_ClearSearch_Click" />
        </StackPanel>

        <DataGrid Grid.Row="2" x:Name="dg_Students" AutoGenerateColumns="False" 
                  SelectionChanged="dg_Students_SelectionChanged"
                  CanUserAddRows="False"
                  IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50" IsReadOnly="True"/>
                <DataGridTextColumn Header="ФИО" Binding="{Binding FullName}" Width="200"/>
                <DataGridTextColumn Header="Группа" Binding="{Binding Group}" Width="100"/>
                <DataGridTextColumn Header="Курс" Binding="{Binding Course}" Width="60"/>
                <DataGridTextColumn Header="Форма обучения" Binding="{Binding StudyForm}" Width="120"/>
                <DataGridTextColumn Header="Дата поступления" Binding="{Binding AdmissionDate, StringFormat={}{0:dd.MM.yyyy}}" Width="120"/>
                <DataGridCheckBoxColumn Header="Активный" Binding="{Binding IsActive}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>

        <GroupBox x:Name="gb_AddEditForm" Grid.Row="3" Header="Данные студента" Margin="0,10,0,0" Visibility="Hidden">
            <Grid Margin="5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="5">
                    <Label Content="ФИО*"/>
                    <TextBox x:Name="tb_FullName" Margin="0,0,0,10" Text="{Binding Path=FullName}"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Margin="5">
                    <Label Content="Группа*"/>
                    <TextBox x:Name="tb_Group" Margin="0,0,0,10" Text="{Binding Path=Group}"/>
                </StackPanel>

                <StackPanel Grid.Column="2" Margin="5">
                    <Label Content="Курс"/>
                    <ComboBox x:Name="cb_Course" Margin="0,0,0,10" Text="{Binding Path=Course}">
                        <ComboBoxItem Content="1"/>
                        <ComboBoxItem Content="2"/>
                        <ComboBoxItem Content="3"/>
                        <ComboBoxItem Content="4"/>
                        <ComboBoxItem Content="5"/>
                    </ComboBox>
                </StackPanel>

                <StackPanel Grid.Column="3" Margin="5">
                    <Label Content="Форма обучения"/>
                    <ComboBox x:Name="cb_StudyForm" Margin="0,0,0,10" Text="{Binding Path=StudyForm}">
                        <ComboBoxItem Content="Очная"/>
                        <ComboBoxItem Content="Заочная"/>
                    </ComboBox>
                </StackPanel>

                <StackPanel Grid.Column="4" Margin="5" VerticalAlignment="Bottom" Panel.ZIndex="0">
                    <Button x:Name="btn_Save" Content="Сохранить" Margin="0,0,0,5" Padding="15,5" 
                            Click="btn_Save_Click" IsEnabled="False" />
                    <Button x:Name="btn_Cancel" Content="Отмена" Padding="15,5" 
                            Click="btn_Cancel_Click"/>
                </StackPanel>
            </Grid>
        </GroupBox>

        <StatusBar Grid.Row="4" Margin="0,5,0,0" Panel.ZIndex="1">
            <TextBlock x:Name="tb_Status" Text="Готово"/>
            <Separator/>
            <TextBlock x:Name="tb_RecordCount" Text="Записей: 0"/>
        </StatusBar>
    </Grid>
</Window>
